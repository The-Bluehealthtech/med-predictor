# Pipeline GitLab CI/CD Minimal
# Version: 4.0 - Test de Base

stages:
  - test

test_minimal:
  stage: test
  image: alpine:latest
  script:
    - echo "Hello World from GitLab CI!"
    - echo "Pipeline fonctionne !"
  rules:
    - if: $CI_COMMIT_BRANCH
